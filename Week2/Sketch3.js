let x,y;
let diameter = 50;
let yVel = 5;
function setup(){
   createCanvas(windowWidth, windowHeight)
   background(100)


}
function draw(){
   background(100)
   ellipse(X,Y,diameter)

   y=5
   if(y == 5){// = just set the value, == is a comparison operator
    y=-5
   }
  
   
}